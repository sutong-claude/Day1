# 集训模拟赛仓库说明（给以后每次会话的 Claude）

这个仓库是小苏同学所有集训模拟赛的复盘仓库，**一个仓库管所有比赛**，不按比赛拆仓库，也不改仓库名。

## 先读这三个文件

1. `README.md`：比赛列表和分数走势
2. `错因档案.md`：跨比赛累积的病因表，判断这场的错是不是“复发”
3. `赛前执行卡.md`：已经定下的比赛规则，复盘时对照“这场有没有照着做”

## 目录约定

```
README.md                  比赛索引 + 分数表
CLAUDE.md                  本文件
赛前执行卡.md               每场赛前读；每场复盘后更新
错因档案.md                 每场复盘后追加
YYYY-MM-DD_比赛名/          每场一个文件夹
  复盘报告.md / .pdf
  正解代码/                 验证过的正解（用户码风）
  赛时代码/                 从虚拟机恢复的各版本，文件名带北京时间
  动画讲义/                 需要时做的交互讲义 HTML
tools/vm/                  run.sh、init.sh、Code::Blocks 核实文档
```

## 每场比赛的复盘流程

**用户会给**：比赛归档助手导出的 ZIP 和“AI 完整上下文”TXT、赛时录音转写（docx）、虚拟机桌面导出（赛前 / 赛后，含回收站和 Downloads 里的大样例）、草稿纸照片。

**要产出**：
1. 新建 `YYYY-MM-DD_比赛名/`，写复盘报告（按 `oi-contest-review-skill` 的结构），正解代码要用大样例和暴力对拍验证。
2. 更新 `错因档案.md`：同一病因再犯要加次数，并点明“第几次复发”。
3. 更新 `赛前执行卡.md`：新病因补规则；已有规则这场没照做的，要在复盘里指出。
4. 更新 `README.md` 的比赛表。
5. 用户要讲解时，用 `interactive-study-notes` 做动画讲义（逐帧动画的数据必须跑真实算法生成）。

## 取证经验（已验证，直接用）

- **Hydro 提交时间**：归档里常写“时间未知”。提交 ID（24 位十六进制）的前 8 位就是 Unix 时间戳，换算成北京时间即可。
- **虚拟机时区**：用户虚拟机曾设成 America/New_York，文件修改时间比北京时间慢 12 小时，归档脚本的“本地时区”也会跟着错。用户修好之前，文件时间要 +12 小时。
- **录音对齐**：用“录音里说了某件事的时刻”和“对应文件的修改时间”对两处以上，算出录音起点。
- **两份 docx 转写**可能是同一段录音的不同转写版本，先比对再用。
- **归档里没有榜单**时，要提醒用户下次勾选“排行榜”。

## 用户的初始机子（2026-09-26 起）

虚拟机桌面固定结构，每场赛后用户把桌面上的东西收进 `DayN/`，再从 `bag/` 复制一份干净的出来：

```
Desktop/
  Day1/ Day2/ ...     历次比赛的全部文件
  bag/                干净的备份：debug/、duipai_T1~T4/、problems/
  debug/              Code::Blocks 调试工程（main.cpp 是一个故意留 bug 的前缀和程序，用来赛前试调试器）
  duipai_T1~T4/       对拍：gen.cpp / ac.cpp / wa.cpp / duipai.sh（必须用 bash 运行，sh 会报错）
  problems/           T1~T4.cpp 模板 + map.txt / paper.txt / thoughts.txt
```

复盘时虚拟机桌面导出按这个结构找文件；Code::Blocks 的设置不在桌面里，在 `~/.config/codeblocks/default.conf`。

## 环境事实（已按源码核实）

- 比赛环境：NOI Linux 2.0（Ubuntu 20.04，GCC 9.3.0），Code::Blocks 20.03。
- Code::Blocks 的四个坑及原因：见 `tools/vm/CodeBlocks四个问题_源码核实版.md`。`给ComputerUseAgent的虚拟机调试手册.md` 和 `本地智能体提示词.md` 已作废，不要再引用。
- 用户不希望让智能体去改虚拟机，环境问题给出“界面里点哪里”的最短办法即可。

## 沟通要求

- 简体中文；重要的先说；排版清楚，不要大段文字。
- 用户说错了要直接指出并给改法。
- 代码遵守用户码风（见 `xiaosu-companion-profile`）。

## Git

- 每次会话在自己的分支上工作。**工作做完后开 PR 合并回默认分支**，否则下一次会话看不到这次的内容。
- 提交信息、PR、代码里不要写模型名称。
