# 赛前执行卡

> 每场模拟赛开赛前读一遍，5 分钟。每场复盘后更新。
> 来源：Day1（2026-09-25，45 / 400）。

## 一、思路上的四个“换”

Day1 证明了一件事：**你的想法质量没问题**（A 题 6 分钟推出核心结论，D 题推出了环模型），分数丢在“想法 → 分数”这一段。所以要换的是下面四个习惯，而不是“更努力地想”。

### ① 先核对前提，再往深处想

每想出一个方案，停 1 分钟，问三句：

1. 数据范围**最后一行**看了吗？有没有 ∑n？
2. 这个方案用 n = 3 手算一遍，对吗？
3. 复杂度按 ∑n 算过吗？每一句 STL 调用是 O(几)？

> Day1：漏看 ∑n ≤ 8000，把目标定成 O(n)，追了 1 小时 50 分。

### ② 从“找正解”换成“找分”

每题读完，**第一件事是列子任务表**，标出“我现在就能拿的分”，先把暴力写完交上，再想正解。暴力还能拿来对拍、打表。

> Day1：C 的两个一行送分 + 暴力 DP = 70，D 的背包 = 45，B 的暴搜 = 15，一共 130 分都是会的东西，一分没拿。

### ③ 从“改代码碰运气”换成“回纸上推”

- 输出超过理论上限或低于下限 → 公式结构错了，**停止调符号**，重新推。
- 同一处正负号改了两次还不对 → 停，用 n = 3 手算每个变量。
- 变量含义写在代码第一行注释里，草稿纸和代码用同一个名字。

> Day1：样例输出 20 &gt; 上限 10 之后，又调了 50 分钟符号；草稿纸上 lst 是顺序、代码里 lst 是逆序。

### ④ 每道题都有预算

开写前说一句：“这题我预计拿 X 分，最多花 Y 分钟。”到点就换题。

> Day1：T1 用了 55% 的时间，拿 20 分；T3 只看了 6 分钟。

## 二、每道题的六步

| 步 | 做什么 | Day1 的例子 |
|---|---|---|
| 读 | 把题意翻译成一句人话；读到数据范围最后一个字；列子任务表 | B：“想给 x 号格子加 1，必须有格子的值等于 x” |
| 玩 | 手玩小例子，写暴力 | D：从大样例找规律，方向是对的 |
| 问 | 什么变了、什么没变？子任务的特殊性质在提示什么？ | B：总和每次 +1，操作次数固定；C：L = n 就是 Σmax |
| 定 | 定复杂度（按 ∑n，每秒约 10⁸）；把变量含义写在纸上 | A：∑n ≤ 8000 → O(n²) 足够 |
| 写 | 先交暴力，再写正解 | |
| 验 | 用暴力对拍；检查答案是否在上下界内 | A：逆序对不可能超过 n(n−1)/2 |

**卡住时按顺序换工具**：打表 → 画图 → 归纳（前 i−1 个好了，加第 i 个）→ 增量（固定一端，另一端移动）→ 分层 → 抽屉 / 反证。

## 三、时间检查点（4 小时赛制）

| 时刻 | 检查 |
|---|---|
| T+0:15 | 四道题都读完了吗？每题的送分子任务列出来了吗？ |
| T+1:30 | 至少两道题的暴力分已经交上去了吗？ |
| T+3:00 | 停止开新题，回头修已经交了但知道有问题的题 |
| 最后 30 分钟 | 每题都交了吗？暴力分都在吗？编译无警告？大样例再跑一遍？数组大小、long long？ |

## 四、突发情况

| 情况 | 规则 |
|---|---|
| 工具出问题 | 5 分钟修不好，立刻退回终端 `g++` 编译运行，不修工具 |
| 发现自己犯了大错 | 在纸上写三行：还剩几分钟？每题最稳能拿几分？接下来 30 分钟做什么分最多？ |
| 脑子里冒出“我是对的” | 花 1 分钟跑一个数据证明，证明不了就是错的 |
| 头昏、心态崩 | 放开键盘 60 秒，喝水，闭眼慢慢呼气；然后先做机械活（写暴力、编译、跑样例） |
| 有 checker / SPJ | 先在 checker 里搜 `NO`、`feasible`、`impossible`，判定条件常常就写在里面 |

## 五、正式考试额外注意

- **必须用 freopen 读写文件**，文件名按题目要求写，模板里别忘了取消注释。
- 编译命令加 `-O2 -Wall`，看一眼警告。

## 六、环境（NOI Linux 2.0 + Code::Blocks 20.03）

- 写题前关掉工程（左边 Projects 里右键 → Close project），否则 F9 编译运行的是工程里的 main.cpp。
- 终端选第一项 `xterm -T $TITLE -e`，调试才不会误报“终端被关闭”。
- 详见 `tools/vm/CodeBlocks四个问题_源码核实版.md`。
