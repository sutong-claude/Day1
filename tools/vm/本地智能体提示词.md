# 给本地智能体的提示词（直接整段复制过去）

```
你现在要帮我修一台用来打 OI 模拟赛的 Linux 虚拟机。我自己也记不清当时具体出了什么问题，下面是从比赛录音里整理出来的症状。请你先观察、复现、确认原因，再动手修；每修一项都要验证。

【环境】
- 宿主机是我的 Windows 电脑；虚拟机是 NOI Linux 2.0（Ubuntu 20.04，GCC 9.3.0），用户名 sutong，桌面在 /home/sutong/Desktop。
- 虚拟机软件是 VMware 还是 VirtualBox，请你先看一下再告诉我。
- 我写代码用 Code::Blocks 和 Sublime Text，桌面上有一个 Code::Blocks 工程 debug（debug.cbp）。

【规矩】
1. 动手前先给虚拟机拍一个快照，名字叫 before-fix-当天日期。
2. 不要删除我桌面上的任何文件夹和文件（problems、duipaiT1～T4、debug 等），只能新增。
3. 需要 sudo 密码时停下来叫我自己输，你不要代输、不要记录。
4. 需要安装软件时先问我。
5. 每改一项，就在 /home/sutong/Desktop/虚拟机修改记录.txt 里追加一行：时间 / 改了什么 / 怎么验证的 / 结果。
6. 全部做完后，把下面第【验收】节的表格逐项填好发给我。

【症状和要做的事，按重要程度排】

1. Code::Blocks 运行的好像不是屏幕上那份代码。
   我当时说过：“这代码好像不是我的”“我不小心删了个大括号，它是怎么编译起来的”“每改一次代码要退出一次编译器才能有用”。
   最可能的原因：打开了 debug 工程，又单独打开了别的 .cpp，按 F9 时编译运行的是工程里的 main.cpp，不是正在编辑的文件。
   要做：
   a. 复现：打开 debug 工程，再单独打开一个故意写错语法的 test.cpp，按 F9，看是不是照样“成功运行”。把结论记下来告诉我。
   b. 在桌面新建 比赛须知.txt，写上：“比赛时编译和测样例一律用终端里的 run.sh；Code::Blocks 只用来断点调试，调试时把代码整份粘进 debug/main.cpp，并确认顶部 Build target 是 Debug。”
   c. 比赛中这个工程被切到过 Release 目标（-O2 -s，没有调试信息，断点停不下来）。把默认目标改回 Debug。
   验证：改 debug/main.cpp 里的一行输出，按 F9，输出要跟着变。

2. Code::Blocks 运行程序时弹出两个窗口、或者一闪就退、F9 没反应。
   要做：Settings → Environment → General settings → “Terminal to launch console programs”，先截图记下原值。
   如果有 gnome-terminal，改成：gnome-terminal --wait -t $TITLE -x
   如果只有 xterm，改成：xterm -T $TITLE -e
   关 Code::Blocks 时如果问是否保存设置，选 Yes。
   验证：跑一个“读入一个数再输出”的程序，只开一个窗口、能输入、结束后停在 Press ENTER to continue。

3. 关 Code::Blocks 时一连串 Yes/No 弹窗。
   原因之一是同一个文件同时在 Sublime 和 Code::Blocks 里开着。
   要做：在 Code::Blocks 设置里找“退出时保存工作区/工程”和“检测到外部修改的文件”相关选项，设成自动、不再询问（不同版本名字不一样，找不到就别硬改，记下来）。在 比赛须知.txt 写上“同一个文件只在一个编辑器里开”。

4. Code::Blocks 没有自动缩进。
   要做：Settings → Editor → 打开 Auto indent 和 Smart indent，Tab 宽度 4，用空格代替 Tab。

5. 终端里复制粘贴不了，每次测样例都要手敲 g++ 和 diff 命令，还经常敲错。
   要做：
   a. 教我：gnome-terminal 里复制是 Ctrl+Shift+C、粘贴是 Ctrl+Shift+V；xterm 里选中就是复制、鼠标中键粘贴；按 ↑ 调出上一条命令；Ctrl+R 搜历史命令。
   b. 在桌面放 run.sh（内容见最后），chmod +x。
   c. 如果虚拟机软件支持，打开宿主机和虚拟机之间的共享剪贴板（VMware：装 open-vm-tools-desktop 并在设置里开共享剪贴板；VirtualBox：装增强功能，设备 → 共享粘贴板 → 双向）。装之前先问我。

6. 鼠标突然没了（被虚拟机抓住）。
   要做：告诉我释放鼠标的热键（VMware 是 Ctrl+Alt，VirtualBox 是右 Ctrl），写进 比赛须知.txt。装好第 5 条的增强工具后，鼠标应该能自由进出。

7. 虚拟机卡顿（终端卡死、虚拟机卡死）。
   注意：我当时以为程序跑 7 秒是因为虚拟机慢，这是我判断错了，那是我代码复杂度的问题，不用为“程序跑得快”去调虚拟机。调资源只是为了界面不卡。
   要做：关掉虚拟机后，CPU 设 4 核（不超过宿主机物理核数的一半），内存 4～8 GB；Windows 电源计划改成高性能。
   验证：开机后在终端连续快速打字 30 秒不卡；nproc 和 free -h 的数和设置一致。

8. 虚拟机时区错了，比北京时间慢 12 小时（设成了纽约）。
   要做：sudo timedatectl set-timezone Asia/Shanghai（sudo 密码叫我输）。
   验证：date 显示的时间和 Windows 右下角一致；浏览器里显示的时间也变成北京时间。

9. Sublime Text 会弹更新提示，我比赛时差点点了。
   要做：Sublime → Preferences → Settings，在右边用户设置里加一行 "update_check": false, 保存。

10. 赛前准备：我每次手工建四个对拍文件夹，结果一次都没用上。
   要做：在桌面放 init.sh（内容见最后）和 template.cpp（用 /home/sutong/Desktop/problems/T3.cpp 那份模板复制一份），chmod +x init.sh。
   验证：在一个临时目录里跑一次 init.sh，能生成 T1～T4 四个文件夹，每个里面有 Tx.cpp 和 run.sh。

【验收】做完后发给我这张表，每行写“做了/没做/做不了”和证据（截图或命令输出）：
| # | 项目 | 验证方法 | 结果 |
| 1 | 快照已拍 | 快照列表截图 | |
| 2 | F9 问题复现结论 + 默认目标是 Debug | 改代码按 F9 输出跟着变 | |
| 3 | 控制台终端设置 | 只弹一个窗口且停住 | |
| 4 | 自动缩进 | 输入 { 回车自动缩进 4 格 | |
| 5 | run.sh 装好并测过 | 在题目文件夹跑 ./run.sh T1 的输出 | |
| 6 | init.sh 装好并测过 | 生成 T1～T4 | |
| 7 | 共享剪贴板 | 宿主机复制的文字能粘进虚拟机终端 | |
| 8 | 鼠标不再被抓住 | 移出窗口不用按热键 | |
| 9 | CPU / 内存 | nproc、free -h 截图 | |
| 10 | 时区 | timedatectl 截图 | |
| 11 | Sublime 不再提示更新 | 设置文件截图 | |
| 12 | 修改记录和比赛须知 | 两个 txt 都在桌面 | |

【run.sh 的内容】放到 /home/sutong/Desktop/run.sh
#!/bin/bash
# 用法（在题目文件夹里）: ./run.sh T1      编译 T1.cpp，跑当前文件夹所有 *.in，和同名 .out/.ans 比对
#                        ./run.sh T1 d    调试模式（-g + 越界/溢出检查）
p=$1
if [ "$2" = d ]; then f="-g -fsanitize=address,undefined"; else f="-O2"; fi
g++ $p.cpp -o $p -std=c++14 -Wall $f || exit 1
for i in *.in; do
    [ -e "$i" ] || { echo "没有 .in 文件"; break; }
    a=${i%.in}.out
    [ -e "$a" ] || a=${i%.in}.ans
    s=$(date +%s%N)
    timeout 10 ./$p < "$i" > my.out
    r=$?
    t=$(( ($(date +%s%N) - s) / 1000000 ))
    if [ $r -eq 124 ]; then echo "$i  TLE (>10s)"
    elif [ $r -ne 0 ]; then echo "$i  RE (返回值 $r)  ${t}ms"
    elif diff -wq my.out "$a" > /dev/null; then echo "$i  OK   ${t}ms"
    else echo "$i  WA   ${t}ms"; fi
done

【init.sh 的内容】放到 /home/sutong/Desktop/init.sh
#!/bin/bash
# 用法：cd ~/Desktop && ./init.sh
for i in 1 2 3 4; do
    mkdir -p T$i
    [ -e T$i/T$i.cpp ] || cp ~/Desktop/template.cpp T$i/T$i.cpp
    cp ~/Desktop/run.sh T$i/
done
echo "T1 到 T4 已建好"
```
